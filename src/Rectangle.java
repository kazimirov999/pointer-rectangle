class Rectangle {
    int length;
    int width;

    Rectangle(int L, int W) {
        length = L;
        width = W;
    }

    int getVolume() {
        return length + width;
    }

    int getVolume2(){
        return 2 * (length + width);
    }
    

















}